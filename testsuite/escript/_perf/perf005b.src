
include "perf";

setup();

var x;

foreach elem in arr
  if ( elem == 5 )
    x := elem;
  elseif ( elem == 37 )
    x := elem;
  elseif ( elem == 40 )
    x := elem;
  else
    x := elem;
  endif
endforeach

print( "done" );
