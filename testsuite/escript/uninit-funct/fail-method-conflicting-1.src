// Child two requirements that are for same method but different parameters: it
// can never be fulfilled: attempt to fulfill Requirement1 fails Requirement2.

class Requirement1()
  uninit function method( this, param0 );
endclass

class Requirement2()
  uninit function method( this, param0, param1 );
endclass

class Child( Requirement1, Requirement2 )
  function Child( this )
  endfunction

  function method( this, param0, param1 )
  endfunction
endclass

Child();
