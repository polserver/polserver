enum class MyEnum
    FOO := 1
endenum

enum class MyEnum // Enums are additive if the identifier already exists.
    FOO := 2, // Ignored, redefinition
    BAR := 3,
endenum

print(MyEnum::FOO); // 1
print(MyEnum::BAR); // 3
