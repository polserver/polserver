var str := "abcd";

var dict := dictionary;
for i := 1 to 4
  dict.insert( str[i], i );
endfor
print( "dict: " + dict );
// everything works fine untill now

foreach v in dict
  print( _v_iter + " -> " + v );
endforeach
