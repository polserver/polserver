// Scope06 - a variable with the same name as a function argument
// cannot be declared at the top level of the function block.

function foo(bar)
    var bar; // expect compile error
endfunction

foo(4);