// Scope09 - a variable with the same name as a function argument
// cannot be declared at the top level of the function block.

function foo( bar )
  if ( 1 )
    var bar; // this is ok
    print( bar );
  endif
endfunction

foo( 4 );
