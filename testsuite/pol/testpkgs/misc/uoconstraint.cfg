#############################################################################
## ApplyConstraint fixture
#############################################################################
#
# Read by testpkgs/misc/test_uomenu.src. ApplyConstraint looks each objtype in
# the list it is given up as an *integer* key in a config file, so the element
# names here are numbers; the leading 0x is honoured by strtoul.
#
# The objtypes are made up. ApplyConstraint never consults itemdesc -- it only
# matches the numbers it is handed against the keys below -- so nothing here
# has to be a real item.
#

Ingredient 0x900001
{
    Amount 5
}

Ingredient 0x900002
{
    Amount 20
}

// No Amount at all: skipped whatever the caller asks for.
Ingredient 0x900003
{
    Name nothing
}

// Amount is a string, not an integer, so the value comparison never happens.
Ingredient 0x900004
{
    Amount plenty
}
