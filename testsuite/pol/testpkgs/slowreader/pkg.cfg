# Named to sort after the client package: testclient.py gives POL 30 seconds to connect
# before it exits, and this test deliberately spends several seconds stalled, which would
# eat that budget if it ran first.
name testslowreader
enabled 1
