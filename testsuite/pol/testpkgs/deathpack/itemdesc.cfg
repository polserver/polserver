// The generated tiledata gives graphic 0x152e layer 4, which is the only body layer besides the
// shirt's 5 that a test can occupy - death sorts worn items per layer, so it needs more than one.
Item 0x30152e
{
  Name    test_pants
  Graphic 0x152e
}
