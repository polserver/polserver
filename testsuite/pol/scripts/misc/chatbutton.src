use os;
use uo;

// The core starts this when a client presses the chat button. It is handed the character that
// pressed it and nothing else, and answers that character so a test can see the button arrived.

program chatbutton( who )
  SendSysMessage( who, "chatbutton ran" );
endprogram
